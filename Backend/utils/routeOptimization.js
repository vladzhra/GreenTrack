const calculateOptimizedRoutes = () => {
    // Mock optimization algorithm
    return [
        { binId: 1, route: 'A -> B -> C' },
        { binId: 2, route: 'D -> E -> F' },
    ];
};

module.exports = { calculateOptimizedRoutes };
